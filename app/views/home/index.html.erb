<div class="hero-unit">
  <h1 class="sub-hero" ><%=current_season.description%> Schedule</h1>
  <h4>below are the dates when parents may begin enrolling students for <%=current_season.description%>.</h4>

  <table id="registration_schedule">
    <thead>
      <tr>
        <th>Student Category</th><th>Date</th>
      </tr>
    </thead>
    <tbody>
    <tr>
      <td>Returning Students who attended during the <%=Season.previous.description%></td> <td><%=current_season.fall_registration_open%></td>
    </tr>
    <tr>
      <td>All New Students (any student that <strong>did not</strong> attend during  <%=Season.previous.description%>)</td> <td><%=current_season.open_enrollment_date%></td>

    </tr>
    <tbody>
  </table>

        <%if current_season.open_enrollment_enabled &&  current_season.status!="Closed"%>
              <p>We are no longer accepting registration for the <%=current_season.description%>. Please visit <a href="www.peterwestbrook.org">Main PWF Site</a> for updated registration schedule for the <%Season.next.description%> season  </p>
        <%end%>
</div>

<div class="login-section">
  <div class="row">
    <div class="span9 offset1" >
      <h2>Login & Registration Instructions</h2>
      <div class="auth-cta">
        <h4>Registered Parents</h4>
        <p>If you have registered for the PWF in the past (even if your child was waitlisted, you should have an account. Please click the Sign In button below. 
        <br/>
        <%=link_to "Sign In", login_path, :class=>"auth btn btn-primary btn-large"%>
        </p>
      </div>

      <div class="auth-cta">
        <h4>New Registrations</h4>
        <%if current_season.open_enrollment_enabled &&  current_season.status!="Closed"%>
          <p>If this is your first time registering please click the Create Account button below 
          <br/>
          <%=link_to "Create Account", new_user_registration_path, :id => "new_registration", :class=>"auth btn btn-primary btn-large" %></p>
        <%end%>
      </div>
    </div>
  </div>
</div>
