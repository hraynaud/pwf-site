<%= simple_form_for(@attendance) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :student_registrations %>
    <%= f.input :session %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
