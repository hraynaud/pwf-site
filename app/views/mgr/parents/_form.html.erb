<%= simple_form_for @parent, :html => { :class => 'form-horizontal' } do |f| %>
  <%= f.input :user_id %>
  <div class="form-actions">
    <%= f.button :submit, :class => 'btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                mgr_parents_path, :class => 'btn' %>
  </div>
<% end %>
