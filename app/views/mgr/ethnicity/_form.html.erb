<%= simple_form_for @ethnicity, :html => { :class => 'form-horizontal' } do |f| %>
  <%= f.input :title %>
  <div class="form-actions">
    <%= f.button :submit, :class => 'btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                mgr_ethnicity_path, :class => 'btn' %>
  </div>
<% end %>
